// ***** Node *****
// res.end, res.writeHead
// require() is a Node function for importing a module

// ***** Express *****
// res.send, res.status, res.set, res.type
// app.use: method by which Express adds middleware
// In Express, the order in which routes and middleware are defined is significant
// Relies on middleware to handle static files and views
// Static middleware delivers content that doesn't require special handling: CSS, images, client-side JS files

// ***** npm *****
// --save-dev: saves in dev dependencies, meaning it won't be installed in production
